阿里云云资源关系知识库 v2.1
# 阿里云云资源关系知识库 v2.1


## 服务索引
ECS - 弹性计算服务
RDS - 关系型数据库
Redis - 缓存数据库
MongoDB - 文档数据库
PolarDB - 云原生数据库
SLB - 传统负载均衡
ALB - 应用型负载均衡
VPC - 专有网络
OSS - 对象存储
NAS - 文件存储
EIP - 弹性公网IP
FC - 函数计算
ACK - 容器服务
RocketMQ - 消息队列
Kafka - 消息流
DNS - 云解析
SLS - 日志服务
CMS - 云监控
WAF - Web防火墙
DDoS - DDoS防护
CR - 容器镜像服务
ECD - 无影云桌面
ESS - 弹性伸缩
CEN - 云企业网
VPN - VPN网关
PrivateZone - 内网DNS
SSL - SSL证书服务
SMS - 短信服务
DCDN - 全站加速
NAT - NAT网关
Elasticsearch - 检索分析服务
SAS - 云安全中心
MSE - 微服务引擎


### 关系类型标准化说明
所有关系统一使用以下5种类型：

1. **contains** (包含)
   - 表示一个资源包含另一个资源
   - 例如：VPC包含VSwitch、服务器组包含后端服务器

2. **depends_on** (依赖)
   - 表示一个资源依赖于另一个资源
   - 例如：ECS依赖于VPC、SLB依赖于VPC

3. **attached_to** (附着/绑定)
   - 表示一个资源物理或逻辑附着到另一个资源
   - 例如：ECS挂载磁盘、EIP绑定到ECS

4. **connected_to** (连接)
   - 表示两个资源之间存在连接关系
   - 例如：SLB连接到后端ECS、应用访问RDS

5. **configured_with** (配置)
   - 表示一个资源配置了某种功能或策略
   - 例如：监听器配置ACL、规则配置转发动作、负载均衡配置WAF

--- 
## 1. ECS - 弹性计算服务
核心资源： 实例 (Instance)
角色： 计算单元，承载应用程序。

### 核心关系
网络归属 (位于)
关系： depends_on (ECS -> VPC)
路径： ECS -> VSwitch -> VPC
描述： ECS实例必须部署在VPC内的VSwitch中，继承VPC的网络隔离。
关联字段： VSwitchId (ECS) -> VpcId (VPC)。
查询示例： "ECS i-123所在的VPC"、"VPC vpc-xxx下有哪些ECS"。

存储挂载 (挂载)
关系： attached_to (ECS -> Disk)
描述： ECS可以挂载多块云盘（系统盘/数据盘），磁盘必须挂载到ECS方可使用。
关联字段： InstanceId (Disk)。
查询示例： "ECS i-123挂载了哪些磁盘"、"磁盘 d-123挂载在哪台ECS上"。

安全配置 (配置)
关系： configured_with (ECS -> SecurityGroup)
描述： ECS实例必须关联至少一个安全组，用于控制网络访问规则。
关联字段： SecurityGroupIds (ECS)。
查询示例： "ECS i-123关联了哪些安全组"。

公网访问 (绑定)
关系： attached_to (ECS -> EIP)
描述： ECS可以绑定弹性公网IP（EIP）以实现公网访问。一台ECS可同时绑定多个EIP。
关联字段： AllocationId (ECS中挂载EIP的网卡信息)。
查询示例： "ECS i-123绑定了哪些EIP"、"EIP eip-xxx绑定了哪个资源"。

### 扩展关系
监控 (被监控)
关系： configured_with (ECS -> CMS)
数据： CPU、内存、磁盘IO、网络流量等指标。

存储快照 (来源于)
关系： depends_on (ECS SystemDisk -> Snapshot)
镜像 (基于)
关系： depends_on (ECS -> Image)

负载均衡后端 (作为)
关系： connected_to (SLB/ALB -> ECS)

函数计算挂载 (使用)
关系： attached_to (FC -> NAS attached to ECS) 间接关系

容器节点 (作为)
关系： contains (ACK Cluster -> ECS)

--- 
## 2. RDS - 关系型数据库
核心资源： 数据库实例 (DBInstance)
角色： 托管式关系型数据库服务（MySQL, PostgreSQL, SQL Server等）。

### 核心关系
网络归属 (位于)
关系： depends_on (RDS -> VPC)
路径： RDS -> VSwitch -> VPC
描述： RDS实例必须部署在VPC内，保证网络隔离和安全。
关联字段： VSwitchId (RDS) -> VpcId (VPC)。
查询示例： "RDS rm-xxx所在的VPC"。

连接与访问
关系： connected_to (ECS/FC/ACK -> RDS)
描述： 应用程序（运行于ECS、FC或ACK容器中）通常内网连接RDS实例。
关联： 基于共同VPC或VPC对等连接，通过RDS的ConnectionString或Endpoint。
查询示例： "哪些ECS连接到了RDS rm-xxx"。

备份存储 (存储于)
关系： configured_with (RDS -> OSS)
描述： RDS的物理备份和日志备份可存储在OSS中。
查询示例： "RDS rm-xxx的备份存在哪个OSS Bucket"。

高可用/只读实例 (关联)
关系： contains (Primary RDS -> Secondary/ReadOnly RDS)
描述： 主实例与灾备实例、只读实例之间的复制关系。
查询示例： "RDS rm-xxx的只读实例有哪些"。

### 扩展关系
监控 (被监控)
关系： configured_with (RDS -> CMS)
数据： 连接数、QPS、TPS、磁盘空间、IOPS等。

白名单 (控制访问)
关系： configured_with (RDS -> IP白名单或安全组)

--- 
## 3. Redis - 缓存数据库
核心资源： 实例 (Instance)
角色： 高性能内存键值存储。

### 核心关系
网络归属 (位于)
关系： depends_on (Redis -> VPC)
路径： Redis -> VSwitch -> VPC
关联字段： VSwitchId (Redis)。

应用访问 (被访问)
关系： connected_to (ECS/FC/ACK -> Redis)
描述： 作为缓存层，被应用程序高频访问。

备份存储 (存储于)
关系： configured_with (Redis -> OSS)

---
## 4. MongoDB - 文档数据库
核心资源： 实例 (Instance)
角色： 托管式NoSQL文档数据库。

### 核心关系
网络归属 (位于)
关系： depends_on (MongoDB -> VPC)
路径： MongoDB -> VSwitch -> VPC

应用访问 (被访问)
关系： connected_to (ECS/FC/ACK -> MongoDB)

存储计算分离 (可选)
关系： depends_on (MongoDB -> ESSD) 依赖于存储类型

---
## 5. PolarDB - 云原生数据库
核心资源： 集群 (DBCluster)、节点 (DBNode)
角色： 云原生关系型数据库，计算、存储分离。

### 核心关系
网络归属 (位于)
关系： depends_on (PolarDB -> VPC)
描述： 计算节点（DBNode）部署在VPC中。

存储层 (使用)
关系： depends_on (PolarDB Cluster -> PolarStore)
描述： 核心特性，存储层独立且共享，实现快速扩展。

读写节点与只读节点 (关联)
关系： contains (Primary Node -> ReadOnly Nodes)

应用访问 (被访问)
关系： connected_to (ECS/FC/ACK -> PolarDB)
关联字段： ClusterEndpoint (读写) / PrimaryEndpoint (读写) / ReaderEndpoint (只读)。

--- 
## 6. SLB - 传统负载均衡
核心资源： 负载均衡实例 (LoadBalancer)
角色： 四层（TCP/UDP）和七层（HTTP/HTTPS）流量分发。

### 核心关系
网络部署 (位于/面向)
关系： depends_on (SLB -> VPC)
描述： SLB可以是私网（部署在VPC内）或公网类型。
关联字段： VpcId (私网SLB)。

后端服务 (分发至)
关系： connected_to (SLB -> ECS/ENI)
描述： SLB监听器（Listener）将流量转发到后端服务器组（BackendServer）中的ECS实例。
关联字段： BackendServers (包含ECS InstanceId和端口)。
查询示例： "SLB lb-xxx的后端服务器有哪些"。

安全与防护 (受保护)
关系： configured_with (SLB -> WAF/DDoS)
描述： 公网SLB可接入WAF进行Web防护，或由DDoS高防保护。

域名绑定 (关联)
关系： configured_with (SLB Listener -> DNS Record)
描述： 七层监听器通常与DNS记录绑定，通过域名访问。

### 转发规则与访问控制 (七层HTTP/HTTPS)
监听器规则 (配置)
关系： configured_with (SLB Listener -> ForwardingRules)
描述： 七层监听器可以配置多个转发规则，基于域名、路径等信息将流量分发到不同的后端服务器组。
关联字段： RuleList (包含规则名称、域名、路径、后端服务器组ID)。
查询示例： "SLB监听器 lsn-xxx 有哪些转发规则"、"规则 rule-xxx 转发到哪个后端服务器组"。

访问控制 (应用)
关系： configured_with (SLB Listener -> ACL)
描述： 监听器可以关联访问控制列表（ACL），用于黑白名单IP限制。
关联字段： AclId (监听器配置)。
查询示例： "监听器 lsn-xxx 的ACL包含哪些IP"、"ACL acl-xxx 被哪些监听器使用"。

访问控制类型 (设置)
关系： configured_with (SLB Listener -> ACL Type)
描述： ACL可以是白名单或黑名单模式。
- 白名单：只允许ACL中的IP访问
- 黑名单：拒绝ACL中的IP访问

规则与ACL的关系 (协同)
关系： configured_with (SLB ForwardingRules -> ACL)
描述： 转发规则和ACL可以同时作用于监听器，流量首先通过ACL过滤，再匹配转发规则。
优先级： ACL过滤（第一层） -> 转发规则匹配（第二层） -> 后端服务器选择

### 转发规则与访问控制 (四层TCP/UDP)
监听器访问控制 (应用)
关系： configured_with (SLB TCP/UDP Listener -> ACL)
描述： 四层监听器同样支持访问控制列表，通过IP黑白名单限制访问。
关联字段： AclId (四层监听器配置)。

### SLB ACL 关联查询（重要）

**查询 SLB 监听器关联的 ACL 列表**：

第一步：使用 `DescribeLoadBalancerListeners` 获取监听器配置
- 监听器对象中的 `AclIds` 属性（数组）包含关联的 ACL 实例 ID 列表
- 同时包含 `AclStatus`（on/off）和 `AclType`（white/black）属性

第二步：使用 `DescribeAccessControlListAttribute` 查询 ACL 详情
- 参数：`--AclId <acl-id>` --RegionId <region>
- 返回：AclEntrys（IP条目）、AclName、RelatedListeners、ResourceGroupId

**与 ALB 的区别**：
- SLB：ACL 关联信息直接在监听器对象的 `AclIds` 数组字段中
- ALB：必须调用 `ListAclRelations` API 才能获取真实的关联关系

健康检查 (监控)
关系： configured_with (SLB Listener -> BackendServer Health)
描述： 监听器通过健康检查探测后端服务器的可用性，自动摘除不健康的服务器。

--- 
## 7. ALB - 应用型负载均衡
核心资源： 负载均衡实例 (LoadBalancer)
角色： 七层高级负载均衡，支持基于内容的路由。

### 核心关系
网络部署 (位于)
关系： depends_on (ALB -> VPC)
描述： ALB通常部署在VPC内，通过EIP或共享带宽提供公网服务。

后端服务 (分发至)
关系： connected_to (ALB -> ECS/ECI/FC/IP)
描述： 支持更丰富的后端类型，包括ECS、ECI容器实例、FC函数、IP等。
查询示例： "ALB alb-xxx的后端服务器组包含哪些资源"。

安全与防护 (受保护)
关系： configured_with (ALB -> WAF)

域名与路由规则 (关联)
关系： configured_with (ALB -> DNS Host + Path Rules)

### 转发规则体系 (核心特性)
监听器 (入口)
关系： configured_with (ALB -> Listeners)
描述： ALB实例可以配置多个监听器，每个监听器监听特定的协议和端口。
关联字段： ListenerPort, ListenerProtocol (HTTP/HTTPS/QUIC)。
查询示例： "ALB alb-xxx 有哪些监听器"、"监听器 lsn-xxx 监听哪个端口"。

转发规则 (路由)
关系： configured_with (ALB Listener -> Rules)
描述： 监听器可以配置多个转发规则，每个规则定义匹配条件和转发动作。
关联字段： RuleConditions (匹配条件) + RuleActions (转发动作)。
查询示例： "监听器 lsn-xxx 有多少条转发规则"、"规则 rule-xxx 匹配哪些条件"。

规则条件 (匹配)
关系： configured_with (ALB Rule -> Conditions)
描述： 转发规则支持多种匹配条件：
- 域名 (Host): 基于请求头中的Host字段
- 路径 (Path): 基于URL路径
- HTTP头部 (Header): 基于请求头中的键值对
- 查询字符串 (QueryString): 基于URL查询参数
- HTTP方法 (Method): GET/POST/PUT/DELETE等
- Cookie: 基于请求中的Cookie
- 源IP (SourceIp): 基于客户端IP地址
查询示例： "哪些规则匹配域名 example.com"、"哪些规则匹配路径 /api/*"。

规则动作 (转发)
关系： configured_with (ALB Rule -> Actions)
描述： 匹配成功后执行的动作：
- 转发至服务器组 (ForwardGroup): 转发到后端服务器组
- 重定向 (Redirect): 30X重定向到指定URL
- 固定响应 (FixedResponse): 返回固定内容
- 请求头重写 (Rewrite): 修改请求头后转发
查询示例： "规则 rule-xxx 转发到哪个服务器组"、"规则 rule-xxx 是否重定向"。

### 服务器组与后端资源
服务器组 (汇聚)
关系： contains (ALB ServerGroup -> BackendServers)
描述： 服务器组包含多个后端服务器，支持多种后端类型：
- ECS实例: 云服务器
- ECI实例: 容器实例
- IP地址: VPC内的IP地址
- 函数计算: Serverless函数
关联字段： ServerGroupId, ServerType, BackendList。
查询示例： "服务器组 srg-xxx 包含哪些后端"、"哪些规则转发到服务器组 srg-xxx"。

服务器组类型 (分类)
关系： contains (ALB ServerGroup -> ServerTypes)
描述： 服务器组分为不同类型：
- 实例类型: 添加ECS或ECI实例
- IP类型: 添加VPC内的IP地址
- IP组类型: 可以跨VPC添加后端

### 访问控制列表 (ACL)

**⚠️ 重要注意事项 - ALB 与 SLB 的 ACL 查询差异**：

**ALB**：监听器 ACL 关联信息**不会存储在监听器对象的 AclId 字段中**，必须通过 `ListAclRelations` API 查询才能获取真实的关联关系！
- 监听器对象返回的 AclId 字段可能为空或不可靠
- 只有在 `ListAclRelations` API 返回结果中出现的监听器才是真正配置了 ACL 的
- 查询 ACL 关联关系时，应优先使用 `ListAclRelations` 而非依赖监听器属性

**SLB**：监听器 ACL 关联信息**直接存储在监听器对象的 `AclIds` 数组字段中**（注意是复数形式）！
- 通过 `DescribeLoadBalancerListeners` API 获取监听器配置
- 监听器对象的 `AclIds` 属性（数组）包含关联的 ACL 实例 ID 列表
- 同时包含 `AclStatus`（on/off）和 `AclType`（white/black）属性
- 使用 `DescribeAccessControlListAttribute` 查询 ACL 详情

访问控制策略 (应用)
关系： configured_with (ALB Listener -> AclEntries)
描述： 监听器可以配置访问控制策略，限制客户端IP访问。
关联字段： AclEntries (包含IP/CIDR条目)。

ACL条目 (条目)
关系： contains (ALB ACL -> Entry)
描述： 每个ACL条目包含：
- IP地址或CIDR网段
- 条目描述
查询示例： "监听器 lsn-xxx 的ACL包含哪些IP"、"IP 1.2.3.4 是否在ACL中"。

ACL与规则的协同 (多层控制)
关系： configured_with (ALB Listener -> ACL -> Rules -> ServerGroup)
描述： 访问控制和转发规则形成多层流量处理：
1. 第一层：ACL过滤 (基于源IP)
2. 第二层：规则匹配 (基于域名、路径、HTTP头等)
3. 第三层：转发到服务器组
优先级： ACL优先级最高，规则按优先级顺序匹配。

### 高级路由特性
规则优先级 (排序)
关系： configured_with (ALB Rules -> Priority)
描述： 转发规则按优先级顺序匹配，优先级为1-10000，数值越小优先级越高。
查询示例： "监听器 lsn-xxx 的规则优先级顺序"。

规则转发动作 (分发策略)
关系： configured_with (ALB Rule -> ForwardAction)
描述： 转发到服务器组时支持多种分发策略：
- 轮询 (RoundRobin)
- 最小连接数 (LeastConnection)
- 源IP哈希 (SourceIpHash)
- 一致性哈希 (consistent_hash)
查询示例： "规则 rule-xxx 使用什么负载均衡算法"。

会话保持 (状态)
关系： configured_with (ALB Rule -> ClientSession)
描述： 转发规则可以配置会话保持，确保同一客户端的请求转发到同一后端。
关联字段： SessionEnabled, SessionTimeout。
查询示例： "规则 rule-xxx 是否启用了会话保持"。

--- 
## 8. VPC - 专有网络
核心资源： 专有网络 (VPC)
角色： 云上私有网络环境的核心枢纽，提供隔离、连接。

### 核心关系 (包含)
包含子网
关系： contains (VPC -> VSwitch)
描述： VPC由至少一个VSwitch（交换机）组成，每个VSwitch属于一个可用区。
查询示例： “VPC vpc-xxx下有哪些VSwitch”。

包含路由表
关系： contains (VPC -> RouteTable)
描述： 控制VPC内流量的转发规则。

包含安全组
关系： contains (VPC -> SecurityGroup)
描述： 安全组是VPC级别的虚拟防火墙。

包含网络ACL
关系： contains (VPC -> NetworkACL) 可选
描述： 子网级别的无状态访问控制列表。

### 核心关系 (承载)
承载计算资源
关系： contains (VPC -> ECS/RDS/Redis/MongoDB/PolarDB/SLB/ALB/ACK Node...)
描述： 绝大多数云产品实例都部署在VPC内，通过VSwitch连接。
查询示例： "VPC vpc-xxx内有哪些云资源"。

连接公网
关系： connected_to (VPC -> NAT Gateway / EIP / Internet Gateway)

连接其他网络
关系： connected_to (VPC -> Other VPC) - VPC对等连接
关系： connected_to (VPC -> VPN Gateway / Express Connect) - 混合云

--- 
## 9. OSS - 对象存储
核心资源： 存储空间 (Bucket)
角色： 海量、安全、低成本的对象存储服务。

### 核心关系
作为数据源/目的地
关系： connected_to (OSS -> ECS/FC/ACK/DataWorks/...)
描述： OSS被广泛用于存储静态文件、备份、日志、大数据源等。
查询示例： "哪些ECS应用向Bucket my-bucket写入数据"。(通常需结合日志或监控推断)

存储备份
关系： configured_with (OSS -> RDS/Redis/ECS Disk Snapshot)
描述： 多个服务的备份功能使用OSS作为存储后端。

托管静态网站
关系： configured_with (OSS Bucket -> DNS Record)
描述： 结合CDN和DNS，OSS可以托管静态网站。

日志存储
关系： configured_with (OSS -> SLS Logstore 投递目标 / ECS日志直传)

---
## 10. NAS - 文件存储
核心资源： 文件系统 (FileSystem)、挂载点 (MountTarget)
角色： 共享文件存储，支持标准文件协议。

### 核心关系
被计算资源挂载
关系： attached_to (NAS -> ECS/FC/ACK/Container)
描述： NAS文件系统通过挂载点（位于VSwitch）被一个或多个ECS实例或容器挂载，实现文件共享。
关联字段： MountTarget 的 VSwitchId 和访问域名/IP。
查询示例： "NAS文件系统 fs-xxx 被哪些ECS挂载"。

网络归属
关系： depends_on (NAS MountTarget -> VSwitch -> VPC)

---
## 11. EIP - 弹性公网IP
核心资源： 弹性公网IP地址 (EIP)
角色： 独立的公网IP资源，可动态绑定和解绑。

### 核心关系
绑定至资源
关系： attached_to (EIP -> ECS/NAT Gateway/SLB/ALB)
描述： EIP可以绑定到拥有公网网卡的云资源上，为其提供固定的公网访问入口或出口。
关联字段： InstanceId (EIP关联信息)。
查询示例： "EIP eip-xxx绑定了哪个实例"、"ECS i-123绑定了哪些EIP"。

共享带宽 (关联)
关系： configured_with (Multiple EIPs -> CommonBandwidthPackage) 可选

--- 
## 12. FC - 函数计算
核心资源： 服务 (Service)、函数 (Function)
角色： 事件驱动的全托管Serverless计算服务。

### 核心关系
网络配置 (位于)
关系： depends_on (FC Service -> VPC) 可选
描述： 函数可以配置VPC访问，从而访问VPC内的RDS、Redis等资源。
关联字段： VpcConfig (FC Service)。

访问其他服务
关系： connected_to (FC Function -> RDS/Redis/OSS/MNS/...)
描述： 函数逻辑中常调用其他云服务API或访问其资源。

事件源 (由...触发)
关系： configured_with (FC Function -> OSS Event/Log Trigger/Timer/API Gateway/...)
查询示例： "函数 my-func 由哪些事件源触发"。

存储挂载 (使用)
关系： attached_to (FC -> NAS) 通过VPC配置挂载NAS

---
## 13. ACK - 容器服务
核心资源： 集群 (Cluster)、节点池 (NodePool)
角色： 托管式Kubernetes容器服务。

### 核心关系
基础设施依赖
关系： depends_on (ACK Cluster -> VPC & VSwitch & ECS/SecurityGroup)
描述： ACK集群的Master和Worker节点（ECS）部署在用户VPC中。
查询示例： "ACK集群 ck-xxx 使用了哪些VSwitch和ECS节点"。

负载均衡
关系： connected_to (ACK Service/Ingress -> SLB/ALB)
描述： Kubernetes Service（类型为LoadBalancer）或Ingress会自动创建或关联SLB/ALB实例。

存储
关系： attached_to (ACK Pod -> Cloud Disk / NAS / OSS Flexvolume)
描述： Pod可以挂载云盘、NAS或OSS作为持久化存储。

配置与密码管理
关系： configured_with (ACK -> KMS/Secrets Manager) 间接

--- 
## 14. RocketMQ - 消息队列
核心资源： 实例 (Instance)、主题 (Topic)、分组 (Group)
角色： 分布式消息队列和微服务总线。

### 核心关系
网络归属
关系： depends_on (RocketMQ -> VPC)
生产者/消费者
关系： connected_to (RocketMQ Topic -> ECS/FC/ACK)
描述： 运行在ECS、FC或ACK上的应用作为消息的生产者或消费者。
查询示例： "Topic my-topic 有哪些消费者客户端"。(通常需结合客户端日志或监控)

---
## 15. Kafka - 消息流
核心资源： 实例 (Instance)、主题 (Topic)
角色： 分布式流处理平台，用于实时数据管道和流分析。
网络归属
关系： depends_on (Kafka -> VPC)

生产者/消费者 (连接)
关系： connected_to (Kafka -> ECS/FLINK/...)
描述： 数据生产端（如日志采集器、业务系统）和消费端（如流计算Flink、数据分析系统）连接Kafka集群。

与SLS关系
关系： configured_with (Kafka <- SLS Logtail) SLS日志投递到Kafka
关系： configured_with (Kafka -> SLS Stream Processing) Kafka作为SLS流计算数据源

---
## 16. DNS - 云解析
核心资源： 域名 (Domain)、解析记录 (Record)
角色： 域名解析和管理。

### 核心关系
指向云资源
关系： connected_to (DNS Record -> SLB/ALB/EIP/OSS Bucket Endpoint/...)
描述： A记录或CNAME记录将域名解析到云服务的公网IP或服务地址。
查询示例： "域名 example.com 的A记录指向哪个SLB或EIP"。

--- 
## 17. SLS - 日志服务
核心资源： 项目 (Project)、日志库 (Logstore)
角色： 一站式日志数据平台。

### 核心关系
采集日志
关系： connected_to (SLS Logstore -> ECS/ACK/FC/RDS/OSS/...)
描述： 通过Logtail客户端、SDK或服务集成，采集几乎所有阿里云服务的日志。
查询示例： "SLS项目 my-project 采集了哪些ECS实例的日志"。

投递日志
关系： configured_with (SLS Logstore -> OSS/FC/...)
描述： 日志可以投递到OSS长期存储，或触发函数计算进行事件处理。

消费日志
关系： configured_with (SLS Logstore -> CloudMonitor Alarms / Stream Processing / Quick BI)

---
## 18. CMS - 云监控
核心资源： 命名空间 (Namespace)、监控项 (Metric)、报警规则 (AlarmRule)
角色： 云资源监控和报警服务。

### 核心关系
监控所有
关系： configured_with (CMS -> 所有其他阿里云资源)
描述： CMS提供几乎所有云服务的基础设施和应用程序监控指标。
查询示例： "查看ECS i-123的监控图表"、"为RDS rm-xxx设置CPU使用率报警"。

报警通知
关系： configured_with (CMS Alarm -> Message Service / DingTalk / Webhook)
描述： 报警触发后，通过消息服务、钉钉等渠道通知。

---
## 19. WAF - Web防火墙
核心资源： 域名 (Domain)、防护规则 (Rule)
角色： Web应用防护，防御SQL注入、XSS等攻击。

### 核心关系
防护Web资源
关系： configured_with (WAF -> SLB/ALB/ECS/EIP/Cloud原生网关)
描述： WAF通过CNAME接入，将流量引流至WAF集群清洗后再回源到实际的Web服务器。
关联： 用户在WAF控制台添加需要防护的域名，该域名通常解析到SLB/ALB/EIP。
查询示例： "域名 example.com 是否接入了WAF防护"、"WAF防护了哪些SLB实例"。

--- 
## 20. DDoS - DDoS防护
核心资源： 防护实例 (Instance)、防护IP (ProtectedIP)
角色： 抵御分布式拒绝服务攻击。

### 核心关系
防护公网IP
关系： configured_with (DDoS Instance -> EIP/公网SLB/公网ALB/Web托管IP)
描述： DDoS高防IP或共享防护包为云上公网IP资源提供DDoS攻击防护。
查询示例： "EIP eip-xxx是否在DDoS防护列表内"。

---
## 21. CR - 容器镜像服务
核心资源： 企业版实例 (Instance)、命名空间 (Namespace)、镜像仓库 (Repository)
角色： 容器镜像的托管、构建和分发平台。

### 核心关系
镜像托管 (包含)
关系： contains (CR Instance -> Namespace -> Repository -> Tag)
描述： 企业版实例包含多个命名空间，每个命名空间包含多个镜像仓库，每个仓库包含多个镜像Tag版本。
查询示例： "CR实例 cri-xxx 有哪些命名空间"、"命名空间 my-ns 下有哪些镜像仓库"。

容器服务关联 (被使用)
关系： connected_to (ACK/ECS -> CR Repository)
描述： ACK集群或ECS上的容器运行时从CR拉取镜像部署应用。
查询示例： "镜像仓库 my-app 被哪些ACK集群使用"。

镜像构建 (触发)
关系： configured_with (CR Repository -> 代码源/构建规则)
描述： 镜像仓库可配置自动构建规则，关联代码仓库（GitHub/GitLab等），代码提交后自动构建镜像。

镜像同步 (复制)
关系： connected_to (CR Instance A -> CR Instance B)
描述： 企业版实例间可配置镜像同步规则，实现跨地域镜像分发。

---
## 22. ECD - 无影云桌面
核心资源： 云桌面 (Desktop)、办公网络 (OfficeSite)
角色： 安全高效的云上桌面办公服务。

### 核心关系
网络归属 (位于)
关系： depends_on (ECD Desktop -> OfficeSite -> VPC)
描述： 云桌面部署在办公网络中，办公网络关联VPC，继承VPC的网络隔离。
查询示例： "云桌面 ecd-xxx 所在的办公网络"、"办公网络下有哪些云桌面"。

桌面组 (包含)
关系： contains (ECD DesktopGroup -> Desktop)
描述： 桌面组包含多个云桌面实例，便于统一管理和分配。

安全策略 (配置)
关系： configured_with (ECD OfficeSite -> SecurityGroup)
描述： 办公网络关联安全组，控制云桌面的网络访问。

---
## 23. ESS - 弹性伸缩
核心资源： 伸缩组 (ScalingGroup)、伸缩规则 (ScalingRule)、伸缩配置 (ScalingConfiguration)
角色： 根据业务需求和策略自动调整计算资源。

### 核心关系
管理ECS实例 (包含)
关系： contains (ESS ScalingGroup -> ECS Instance)
描述： 伸缩组管理一组ECS实例，根据伸缩规则自动增减实例数量。
关联字段： ScalingGroupId (ECS实例通过伸缩组创建和管理)。
查询示例： "伸缩组 asg-xxx 包含哪些ECS实例"、"ECS i-xxx 属于哪个伸缩组"。

网络归属 (位于)
关系： depends_on (ESS ScalingGroup -> VPC/VSwitch)
描述： 伸缩组关联VPC和VSwitch，新创建的ECS实例部署在指定网络中。

负载均衡关联 (连接)
关系： connected_to (ESS ScalingGroup -> SLB/ALB)
描述： 伸缩组可关联SLB/ALB实例，新增ECS自动加入负载均衡后端，移除时自动摘除。
关联字段： LoadBalancerIds (SLB)、AlbServerGroups (ALB)。
查询示例： "伸缩组 asg-xxx 关联了哪些SLB"。

数据库关联 (连接)
关系： connected_to (ESS ScalingGroup -> RDS)
描述： 伸缩组可关联RDS实例，新增ECS的IP自动加入RDS白名单。
关联字段： DBInstanceIds。

伸缩配置 (配置)
关系： configured_with (ESS ScalingGroup -> ScalingConfiguration)
描述： 伸缩配置定义新创建ECS实例的规格、镜像、安全组等参数。

伸缩规则 (配置)
关系： configured_with (ESS ScalingGroup -> ScalingRule)
描述： 伸缩规则定义扩缩容的触发条件和调整方式（简单规则、步进规则、目标追踪规则）。

定时任务 (触发)
关系： configured_with (ESS ScalingGroup -> ScheduledTask)
描述： 定时任务按计划触发伸缩规则，实现定时扩缩容。

---
## 24. CEN - 云企业网
核心资源： 云企业网实例 (CenInstance)、转发路由器 (TransitRouter)
角色： 跨地域、跨VPC的全球网络互通服务。

### 核心关系
网络实例挂载 (包含)
关系： contains (CEN Instance -> VPC/VBR/CCN)
描述： 云企业网实例可挂载多个VPC、边界路由器(VBR)和云连接网(CCN)，实现互通。
关联字段： CenId -> ChildInstances (VPC/VBR/CCN)。
查询示例： "CEN cen-xxx 挂载了哪些VPC"、"VPC vpc-xxx 加入了哪个云企业网"。

转发路由器 (路由枢纽)
关系： contains (CEN Instance -> TransitRouter)
描述： 每个地域部署一个转发路由器(TR)，作为该地域的路由枢纽，管理VPC附加和路由策略。
查询示例： "CEN cen-xxx 在北京地域的转发路由器"。

VPC附加 (连接)
关系： connected_to (TransitRouter -> VPC)
描述： 转发路由器通过VPC附加(Attachment)连接VPC，实现跨VPC互通。
查询示例： "转发路由器 tr-xxx 连接了哪些VPC"。

路由管理 (配置)
关系： configured_with (TransitRouter -> RouteTable -> RouteEntry)
描述： 转发路由器包含路由表，路由表包含路由条目，控制跨网络的流量转发。

带宽包 (配置)
关系： configured_with (CEN Instance -> BandwidthPackage)
描述： 跨地域互通需要购买带宽包，控制跨地域流量的带宽上限。
查询示例： "CEN cen-xxx 的跨地域带宽配置"。

---
## 25. VPN - VPN网关
核心资源： VPN网关 (VpnGateway)、IPsec连接 (VpnConnection)、用户网关 (CustomerGateway)
角色： 提供加密的网络连接，实现混合云和远程接入。

### 核心关系
网络归属 (位于)
关系： depends_on (VPN Gateway -> VPC)
描述： VPN网关部署在VPC内，为VPC提供加密的对外连接能力。
关联字段： VpcId (VPN Gateway)。
查询示例： "VPC vpc-xxx 下有哪些VPN网关"。

IPsec连接 (连接)
关系： connected_to (VPN Gateway -> CustomerGateway)
描述： VPN网关通过IPsec连接与用户网关（对端设备）建立加密隧道，实现站点到站点的VPN互联。
关联字段： VpnGatewayId + CustomerGatewayId -> VpnConnection。
查询示例： "VPN网关 vpn-xxx 有哪些IPsec连接"、"连接 vco-xxx 的状态"。

用户网关 (对端)
关系： connected_to (VpnConnection -> CustomerGateway)
描述： 用户网关代表本地数据中心的VPN设备，记录对端公网IP和配置。
查询示例： "用户网关 cgw-xxx 的IP地址"。

SSL-VPN (远程接入)
关系： configured_with (VPN Gateway -> SslVpnServer -> SslVpnClientCert)
描述： VPN网关可配置SSL-VPN服务端，为远程用户提供点到站点的VPN接入。
查询示例： "VPN网关 vpn-xxx 的SSL-VPN配置"。

CEN关联 (连接)
关系： connected_to (VPN Gateway -> CEN TransitRouter)
描述： VPN网关可作为CEN的网络实例挂载到转发路由器，实现VPN流量通过CEN转发。

---
## 26. PrivateZone - 内网DNS
核心资源： 内网域名 (Zone)、解析记录 (Record)
角色： 基于VPC的私有DNS解析服务。

### 核心关系
VPC关联 (绑定)
关系： attached_to (PrivateZone Zone -> VPC)
描述： 内网域名需要关联VPC才能生效，一个Zone可以关联多个VPC，实现跨VPC的内网域名解析。
查询示例： "Zone example.internal 关联了哪些VPC"、"VPC vpc-xxx 关联了哪些内网域名"。

解析记录 (包含)
关系： contains (PrivateZone Zone -> Record)
描述： Zone包含多条解析记录（A、CNAME、MX等），将内网域名解析到VPC内的资源IP。
查询示例： "Zone example.internal 有哪些解析记录"。

指向云资源 (连接)
关系： connected_to (PrivateZone Record -> ECS/SLB/RDS/Redis/...)
描述： 解析记录的值通常指向VPC内的云资源内网IP或服务地址。
查询示例： "域名 db.example.internal 解析到哪个RDS实例"。

---
## 27. SSL - SSL证书服务
核心资源： 证书 (Certificate)
角色： SSL/TLS证书的申请、管理和部署。

### 核心关系
部署到云资源 (配置)
关系： configured_with (SSL Certificate -> SLB/ALB/CDN/DCDN/WAF)
描述： SSL证书可部署到负载均衡HTTPS监听器、CDN/DCDN加速域名、WAF防护域名等。
查询示例： "证书 xxx 部署到了哪些资源"、"ALB alb-xxx 使用了哪个SSL证书"。

域名关联 (绑定)
关系： attached_to (SSL Certificate -> Domain)
描述： 证书绑定到特定域名，一个证书可以是单域名、多域名或通配符证书。
查询示例： "域名 example.com 使用了哪个证书"、"证书是否即将过期"。

---
## 28. SMS - 短信服务
核心资源： 短信模板 (Template)、短信签名 (SignName)
角色： 提供短信发送能力，支持验证码、通知和营销短信。

### 核心关系
应用调用 (被使用)
关系： connected_to (ECS/FC/ACK Application -> SMS)
描述： 运行在ECS、FC或ACK上的应用通过SMS API发送短信。
查询示例： "短信模板 SMS_xxx 的审核状态"。

签名与模板 (配置)
关系： configured_with (SMS SignName -> Template)
描述： 发送短信需要签名和模板配合使用，签名标识发送方，模板定义内容格式。

---
## 29. DCDN - 全站加速
核心资源： 加速域名 (Domain)、配置 (Config)
角色： 融合动静态内容加速的全站加速服务。

### 核心关系
回源配置 (连接)
关系： connected_to (DCDN Domain -> SLB/ALB/ECS/OSS)
描述： DCDN加速域名的回源地址通常指向SLB/ALB/ECS的公网IP或OSS Bucket域名。
查询示例： "DCDN域名 example.com 的回源地址"。

域名解析 (关联)
关系： configured_with (DCDN Domain -> DNS CNAME)
描述： 加速域名需要在DNS中配置CNAME记录指向DCDN分配的加速域名。

证书配置 (配置)
关系： configured_with (DCDN Domain -> SSL Certificate)
描述： HTTPS加速域名需要配置SSL证书。
查询示例： "DCDN域名 example.com 的证书信息"。

WAF防护 (配置)
关系： configured_with (DCDN Domain -> WAF)
描述： DCDN可开启边缘WAF防护功能，在边缘节点进行Web攻击防护。

---
## 30. NAT - NAT网关
核心资源： NAT网关 (NatGateway)、SNAT条目 (SnatEntry)、DNAT条目 (ForwardEntry)
角色： 提供网络地址转换能力，实现VPC内资源的公网访问和端口映射。

### 核心关系
网络归属 (位于)
关系： depends_on (NAT Gateway -> VPC)
描述： NAT网关部署在VPC内，为VPC内的资源提供公网访问能力。
关联字段： VpcId (NAT Gateway)。
查询示例： "VPC vpc-xxx 下有哪些NAT网关"。

EIP绑定 (绑定)
关系： attached_to (NAT Gateway -> EIP)
描述： NAT网关需要绑定EIP才能提供公网访问，SNAT和DNAT条目使用绑定的EIP地址。
查询示例： "NAT网关 ngw-xxx 绑定了哪些EIP"。

SNAT出公网 (配置)
关系： configured_with (NAT Gateway -> SnatEntry -> VSwitch/ECS)
描述： SNAT条目定义VSwitch或ECS实例通过NAT网关访问公网的规则，指定使用的EIP。
查询示例： "NAT网关 ngw-xxx 的SNAT规则"、"VSwitch vsw-xxx 通过哪个NAT出公网"。

DNAT端口映射 (配置)
关系： configured_with (NAT Gateway -> ForwardEntry -> ECS)
描述： DNAT条目将EIP的特定端口映射到VPC内ECS实例的端口，实现公网到内网的端口转发。
查询示例： "NAT网关 ngw-xxx 的DNAT规则"、"哪个端口映射到了ECS i-xxx"。

---
## 31. Elasticsearch - 检索分析服务
核心资源： 实例 (Instance)
角色： 托管式Elasticsearch搜索和分析服务。

### 核心关系
网络归属 (位于)
关系： depends_on (Elasticsearch Instance -> VPC/VSwitch)
描述： ES实例部署在VPC内，通过VSwitch连接。
查询示例： "ES实例 es-xxx 所在的VPC"。

数据源 (被写入)
关系： connected_to (ECS/FC/ACK/SLS -> Elasticsearch)
描述： 应用程序或日志服务将数据写入ES进行索引和搜索。SLS Logstore可配置投递到ES。
查询示例： "ES实例 es-xxx 的集群状态"。

Kibana (配置)
关系： configured_with (Elasticsearch Instance -> Kibana)
描述： ES实例内置Kibana可视化组件，用于数据查询和仪表盘展示。

---
## 32. SAS - 云安全中心
核心资源： 资产 (Instance)、漏洞 (Vul)、告警事件 (AlarmEvent)
角色： 主机安全、漏洞管理、威胁检测和安全态势感知。

### 核心关系
资产防护 (监控)
关系： configured_with (SAS -> ECS/ACK/ECD)
描述： 云安全中心通过Agent监控ECS、ACK节点、云桌面等资产的安全状态。
查询示例： "云安全中心管理了哪些资产"、"ECS i-xxx 的安全状态"。

漏洞检测 (扫描)
关系： configured_with (SAS Vul -> ECS/ACK)
描述： 定期扫描资产上的系统漏洞、应用漏洞和Web-CMS漏洞。
查询示例： "有哪些高危漏洞需要修复"、"ECS i-xxx 有哪些漏洞"。

告警事件 (检测)
关系： configured_with (SAS AlarmEvent -> ECS/ACK)
描述： 实时检测入侵行为、异常登录、恶意进程等安全威胁并产生告警。
查询示例： "最近有哪些安全告警"、"ECS i-xxx 的告警事件"。

基线检查 (审计)
关系： configured_with (SAS Baseline -> ECS)
描述： 检查资产的安全配置是否符合等保、CIS等安全基线标准。

---
## 33. MSE - 微服务引擎
核心资源： 云原生网关 (Gateway)、注册配置中心 (Cluster)
角色： 提供微服务治理能力，包括云原生网关和注册配置中心。

### 核心关系
网络归属 (位于)
关系： depends_on (MSE Gateway/Cluster -> VPC)
描述： MSE网关和注册中心部署在VPC内。

流量入口 (路由)
关系： connected_to (MSE Gateway -> ACK/ECS Service)
描述： 云原生网关作为微服务的统一流量入口，将请求路由到ACK集群或ECS上的后端服务。
查询示例： "MSE网关 gw-xxx 的路由配置"。

服务注册 (连接)
关系： connected_to (ECS/ACK Application -> MSE Cluster)
描述： 微服务应用注册到MSE的Nacos/ZooKeeper集群，实现服务发现和配置管理。
查询示例： "Nacos集群 mse-xxx 注册了哪些服务"。

负载均衡关联 (连接)
关系： connected_to (MSE Gateway -> SLB/ALB)
描述： MSE网关可通过SLB/ALB暴露公网访问入口。

---
## 附录：ALB与SLB转发规则对比

### 转发规则架构对比

**SLB (传统负载均衡)**
- 七层监听器支持域名和路径转发
- 转发规则相对简单，主要基于域名和路径
- 一个监听器可以配置多个转发规则
- 规则匹配后转发到不同的后端服务器组
- ACL与规则协同：ACL过滤 -> 规则匹配 -> 后端选择

**ALB (应用型负载均衡)**
- 更强大的转发规则引擎
- 支持丰富的匹配条件：域名、路径、HTTP头、查询字符串、Cookie、源IP、HTTP方法
- 支持多种转发动作：转发、重定向、固定响应、请求头重写
- 规则优先级可配置（1-10000）
- 服务器组支持多种后端类型：ECS、ECI、IP、FC
- ACL与规则协同：ACL过滤 -> 规则匹配（按优先级） -> 服务器组选择

